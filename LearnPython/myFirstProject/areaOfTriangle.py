# to calculate the area of triangle with base and height

base = float(input("Enter the base of the triangle in cm: "))
height = float(input("can you input the height of the triangle in cm: "))

area = 1/2 * base * height

print(f"The area of the triangle is {area:.2f} sq. cm")